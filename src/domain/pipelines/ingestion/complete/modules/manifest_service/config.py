from src.domain.pipelines.ingestion.pipeline_entities.pathes import ROOT


PATH_TO_MANIFEST_FILE = ROOT / 'src' /'domain' / 'pipelines' / 'ingestion' / 'modules' / 'manifest_service' / 'manifest.db'

